# P3 一句话安装使用实施计划（2026年9月16日）

> 来源：用户追问"是否可以一句话进行安装使用"
> 用户决策（已确认）：**安装=一行命令，装完脚本直接输出5句可用话；README 置顶"一句话安装使用"块；handbook/faq 只加指向，不重复全文**
> 前置基线：P2 已全部落地（skills1.2/速查表三处/安装幂等+100755+LF锁定/门禁全绿，未提交）
> 本文件为执行跟踪表，随执行情况和结果动态更新状态。

图例：⬜ 待开始 / 🟡 进行中 / ✅ 已完成 / ❌ 受阻 / ⏭️ 跳过（本轮不做，留后轮）。

## 状态总览

| 阶段 | 内容 | 状态 | 备注 |
|------|------|------|------|
| P3-1 | 安装脚本成功后输出一句话上手（5 句+用法） | ✅ 已完成 | 实跑尾部5句齐；附带修BOM坑 |
| P3-2 | README"一句话安装使用"块 + handbook/faq 一行指向 | ✅ 已完成 | 命令与冻结一致 |
| P3-3 | Win 实跑 + 门禁归档 | ✅ 已完成 | 四门全绿，见执行日志 |

## 勘察结论（冻结）

1. 安装已是一行命令/每端；缺的是"装完即会用"——脚本成功只输出 ALL 15 LINKS OK，用户还得翻文档找话术。
2. 输出文案与 handbook§5 速查表同字，脚本内嵌（运行期输出，不算文档双源）。
3. 不新增 Skill，不改触发语，不改安装逻辑本身。

## P3-1 拆解

| 子项 | 文件 | 改法 | 状态 |
|------|------|------|------|
| P3-1a | `scripts/install-skills.ps1` | 成功尾输出 5 句 + "把任一句发给 Agent 即可开始，不用敲 /命令" | ✅ 已完成（另补 UTF-8 BOM，见教训） |
| P3-1b | `scripts/install-skills.sh` | 同文案英文标点一致的输出（中文句不变） | ✅ 已完成（另统一工作区为 LF） |

验收：Win 实跑 ps1 尾部含 5 句；sh 逐行复核输出一致。

## P3-2 拆解

| 子项 | 文件 | 改法 | 状态 |
|------|------|------|------|
| P3-2a | `README.md` | 快速开始前加"一句话安装使用"块：安装一行（分 Win/mac 页签式双代码块已有，收敛为上下两块+一句"装完照打勾话术说"）+ 首推第一句 | ✅ 已完成 |
| P3-2b | `docs/handbook.md` §2.3、 `docs/faq.md` Q1 | 各加一行"装完脚本会直接给出 5 句可用话"指向，不重复全文 | ✅ 已完成 |

验收：README 块内安装命令与 P2 冻结一致；无新增长期维护的重复全文。

## P3-3 拆解

| 子项 | 内容 | 状态 |
|------|------|------|
| P3-3a | ps1 实跑：15 SKIP/OK + 尾部 5 句输出 | ✅ 已完成（尾部5句齐；中途修BOM解析错） |
| P3-3b | 门禁：格式/死链复查/可读/数量（Skill恒5） | ✅ 已完成（行尾空格0/死链0/变更集预期/Skill恒5/sh100755） |

## 延后（本轮不做，仅跟踪）

* 远程一键 bootstrap（curl 克隆+安装）——需发布与信任链，下轮再议。
* 让 Agent 读一句话自动执行安装——各 Agent 沙箱权限不一，不做统一承诺。

## 执行日志

* 2026-09-16：建表，开始 P3。
* 2026-09-16：P3-1/P3-2 完成：双脚本成功尾输出 5 句（与 handbook§5 同字）；README"一句话安装使用"块 + 首推第一句；handbook§2.3/faqQ1 各一行指向。
* 2026-09-16：P3-3 实跑修错：ps1 新增中文输出后报解析错（字符串未闭合，行号漂移 2 行）。根因：文件无 BOM，PS5.1 按 GBK 解码，`”"` 结尾的字节把收尾英文引号吞掉。修复：ps1 补 UTF-8 BOM（EF BB BF），sh 工作区统一 LF；重跑 15 SKIP + ALL 15 LINKS OK + 尾部 5 句齐。教训：凡含中文的 ps1 必须带 BOM 落盘；报错行号漂移时优先怀疑编码。
* 2026-09-16：中途发现用户于 21:23 提交了 P1（939622a 删 30 副本 + 7b0dcb1 落盘 P1 全量，22 文件）。核查：HEAD 内容与本工作树 P1 基线一致，无冲突，P2/P3 增量在其上干净叠加；暂存区有用户 `add` 痕迹，不碰，由用户自行 commit。
* 2026-09-16：P3 门禁全绿——[门1]✅行尾空格0；[门2]✅死链0；[门3]✅新增段可读+实跑 5 句齐；[门4]✅变更集预期+Skill恒5+sh100755。综合✅全绿。

## 最终总结

* P3 3/3 完成：安装=一行命令，装完脚本直接吐 5 句可用话；README 置顶"一句话安装使用"块；附带修 ps1-BOM 解析坑。
* 遗留：sh 未在 mac/实机 bash 运行；P2/P3 更改未提交（暂存区有用户 add 痕迹，不碰）。
