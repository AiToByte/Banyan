# P2 Skill 一句话易用性 + Win/macOS 适配实施计划（2026年9月16日）

> 来源：用户需求"使用方式更方便，一句话而非一个指令，同时适配 win/macOS"
> 用户决策（已确认）：**只丰富触发语与文档，不新增 Skill；description 可改（触发记忆影响可接受），正文指令不动；安装保持双脚本但文档给一行命令**
> 前置基线：P1 已全部落地（README 双语/docs6文件/skills1.1/15链接Junction/CLAUDE入口节/MIT/门禁全绿，未提交）
> 本文件为执行跟踪表，随执行情况和结果动态更新状态。

图例：⬜ 待开始 / 🟡 进行中 / ✅ 已完成 / ❌ 受阻 / ⏭️ 跳过（本轮不做，留后轮）。

## 状态总览

| 阶段 | 内容 | 状态 | 备注 |
|------|------|------|------|
| P2-1 | 丰富 5 个 SKILL description 一句话触发语，升 1.2 | ✅ 已完成 | 口语键齐，职责首句保留 |
| P2-2 | 一句话速查表进 README/handbook/faq | ✅ 已完成 | 三处同文案，handbook为源 |
| P2-3 | 安装一键化（一行命令）+ sh 加固 + chmod +x | ✅ 已完成 | SKIP幂等+100755+.gitattributes锁LF |
| P2-4 | Win 实跑验证（ps1 幂等 15 OK + sh 语法检查） | ✅ 已完成 | ps1实跑全SKIP+15OK；sh人工复核 |
| P2-5 | 文档型门禁全绿并归档 | ✅ 已完成 | 四门全绿，见执行日志 |

## 勘察结论（冻结）

1. 现状触发语多为"术语式"（如"拆解任务""追加日志"），口语一句话（如"帮我先出个方案""记一下刚才的报错"）覆盖不足，是"还要敲指令"体感的主因。
2. `banyan-resume` 触发语已最丰富（继续任务/断点恢复/接着做/上次做到哪了/恢复/接力/从上次继续），P2 以它为标杆补齐其余 4 个。
3. 安装双脚本已存在；macOS 侧缺：bash 3.2 兼容确认、可执行位、"一行命令"文档入口。Windows 侧 ps1 已实跑 OK。
4. 本轮不新增 Skill、不改 SKILL 正文指令、不改状态机与门禁逻辑。

## P2-1 拆解：description 一句话触发语（5 文件，只改两处）

| 子项 | 文件 | 改法 | 状态 |
|------|------|------|------|
| P2-1a | 5×`skills/*/SKILL.md` | description 追加口语触发句；版本 1.1→1.2 | ✅ 已完成 |
| P2-1b | 5×`skills/*/SKILL.md` | 正文"何时触发/不触发"节同步补一句（保持与 description 一致） | ✅ 已完成（核查：正文无独立触发节，触发语只放 description，符合渐进披露，不额外加节） |

口语触发句（冻结）：
- draft：用户说"先出个方案再动手""帮我理一下怎么做""从零开始做XX""给个步骤"时触发。
- track：用户说"这步做完了/开始做下一步/卡住了/进度更新下"时触发。
- gate：用户说"帮我检查下/跑下测试看看/能不能合了"时触发。
- log：用户说"记一下/刚才的报错记下来/备忘一下"时触发。
- resume：已够，加"换电脑了继续/新窗口继续"。

验收：grep 每 skill 新增口语键≥3 个；description 仍首句讲清职责（渐进披露不受损）。

## P2-2 拆解：一句话速查表

| 子项 | 文件 | 改法 | 状态 |
|------|------|------|------|
| P2-2a | `docs/handbook.md` §5 开头 | 新增"一句话速查表"（5 行：场景→复制即用的话），为唯一源 | ⬜ 待开始 |
| P2-2b | `README.md` | 快速开始旁加同表精简版（同文案，注"与 handbook §5 同步"） | ⬜ 待开始 |
| P2-2c | `docs/faq.md` | 新增 Q9"能不能不敲指令，一句话调用"（答案指向速查表） | ⬜ 待开始 |

一句话文案（冻结）：
- 规划："这个需求先出方案再动手，拆成步骤存下来。"
- 跟踪："这步做完了（/卡住了），进度更新下。"
- 验收："这步改完了，跑下门禁看看能不能合。"
- 记录："刚才的改动和报错记一下。"
- 接力："继续任务，直接从断点往下做。"

验收：三处文案逐字一致；handbook 目录/faq 编号连续。

## P2-3 拆解：安装一键化 + 跨平台加固

| 子项 | 文件 | 改法 | 状态 |
|------|------|------|------|
| P2-3a | `scripts/install-skills.sh` | 加固：缺 `skills/` 源时报错退出；已是正确链接时跳过重建（幂等输出）；兼容 macOS bash 3.2（不用关联数组/新特性，已确认只用 for/case，补注释声明） | ✅ 已完成（只用for/test/readlink，全POSIX+bash3.2安全） |
| P2-3b | `scripts/install-skills.ps1` | 同等加固（缺源报错；正确 Junction 跳过，输出 SKIP/OK） | ✅ 已完成（实跑15 SKIP+15 OK） |
| P2-3c | `scripts/install-skills.sh` | 置可执行位（git update-index --chmod=+x） | ✅ 已完成（ls-files确认100755，已暂存） |
| P2-3d | `README.md` + `docs/handbook.md` §2.3 | 安装入口收敛为"一行命令"：Win 一行 / mac一行，手动步骤折叠为备注 | ✅ 已完成（README双代码块并列；handbook已有脚本首选，P2-3e补.gitattributes锁sh为LF防mac shebang坏） |

一行命令（冻结）：
- Win：`powershell -ExecutionPolicy Bypass -File scripts/install-skills.ps1`
- mac：`bash scripts/install-skills.sh`

验收：sh 在 Win 侧 `bash -n` 语法通过（如有 git-bash，否则注明未验）；ps1 重跑输出含 SKIP 且 15 OK；`git ls-files -s` 显示 sh 为 100755。

## P2-4 拆解：Win 实跑验证

| 子项 | 内容 | 状态 |
|------|------|------|
| P2-4a | ps1 重跑：幂等（SKIP/OK）+ ALL 15 LINKS OK | ✅ 已完成（15 SKIP+15 OK） |
| P2-4b | sh `bash -n` 语法检查 +逐行复核 macOS 兼容（rm/ln/mkdir 均为 POSIX） | ✅ 已完成（本机无可用bash/WSL无发行版/git-bash缺失，改人工逐行复核：仅for/test/readlink/echo，全bash3.2安全；未实跑如实记录） |

验收：两项输出落 EXEC_LOG。

## P2-5 拆解：验证门禁

| 子项 | 内容 | 状态 |
|------|------|------|
| P2-5a | 门1格式：md 无格式错 | ✅ 已完成（行尾空格0） |
| P2-5b | 门2静态：速查表三处一致 + description 口语键 grep + 死链复查 | ✅ 已完成（关键句三处命中；口语键5文件全；死链0；frontmatter5/5；1.2六处=5SKILL+faq注） |
| P2-5c | 门3可读：抽查 handbook/faq/README 新增段 | ✅ 已完成（新增表/Q9/双代码块可读） |
| P2-5d | 门4数量：变更文件数 + Skill 恒 5 落盘 | ✅ 已完成（M13/??2，Skill恒5，sh100755） |

验证门禁（每阶段）：`markdown 格式检查` + `死链与frontmatter校验`（新告警零容忍）+ `全文件可读抽查`。

## 延后（本轮不做，仅跟踪）

* 新增第 6 个 Skill（如统一路由 banyan-do）——本轮只做触发语与文档。
* CI 自动校验速查表一致性脚本——本轮人工 grep，有需要下轮做。
* macOS 实机运行 sh——本机为 Windows，只做语法+逐行复核。

## 执行日志

* 2026-09-16：建表，开始 P2。
* 2026-09-16：P2-1 完成：5×SKILL description 追加口语触发句并升 1.2（draft+4/track+4/gate+3/log+2/resume+2），职责首句保留；正文无独立触发节，不另加节。
* 2026-09-16：P2-2 完成：一句话速查表落 handbook§5（源）+ README（同文案）+ faq Q9，关键句三处逐字命中。
* 2026-09-16：P2-3 完成：双脚本缺源报错+正确链接SKIP；sh 100755 暂存；新增 `.gitattributes` 锁 `scripts/*.sh eol=lf`（防 Win 检出 CRLF 搞坏 mac shebang）；README 安装收敛为双一行命令。
* 2026-09-16：P2-4 完成：ps1 重跑 15 SKIP + ALL 15 LINKS OK；本机无可用 bash（WSL 无发行版、git-bash 缺失），sh 改人工逐行复核通过，未实跑如实记录。
* 2026-09-16：P2-5 门禁全绿——[门1]✅行尾空格0；[门2]✅速查表三处一致/口语键5文件全/死链0/frontmatter5/5；[门3]✅新增段可读；[门4]✅变更集预期+Skill恒5+sh100755。综合✅全绿。

## 最终总结

* P2 5/5 完成：一句话触发（description 口语化 1.2）+ 一句话速查表（三处同文案）+ 安装一行命令（Win/mac 并列，加固幂等，sh 可执行，LF 锁定）。
* 遗留：sh 未在 mac/实机 bash 运行；P1+P2 更改均未提交，待用户 review 后 commit。
