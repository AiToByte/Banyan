# 任务执行与断点续传协议 (Task Continuity Protocol)

你必须严格遵守此协议。所有复杂任务的状态必须下沉到本地文件，禁止依赖会话记忆。

## 强制文件
- `TASK_PLAN.md`：总体执行计划 + 实时进度状态机
- `EXEC_LOG.md`：详细执行流水账

## 阶段 1：规划（任何代码修改前必须执行）
1. 分析需求，拆解为 4–8 个可独立验证的原子步骤。
2. 立即创建/覆盖 `TASK_PLAN.md`，使用以下严格格式：

```markdown
# 任务总体执行规划: [任务标题]

> 创建/更新时间: YYYY-MM-DD HH:MM
> 当前状态: 规划完成 / 执行中 / 已完成

## 执行进度清单
- [ ] **步骤 1**: [简要描述]（可验证标准：……）
- [ ] **步骤 2**: [简要描述]（可验证标准：……）
...

## 关键决策与约束
- [技术选型、边界、禁止事项]
- 规划完成后简要确认，再进入执行。禁止跳过规划直接改代码。


## 阶段 2：执行与实时日志（强制）

* 开始步骤：把对应 [ ] 改为 [-]，并在 EXEC_LOG.md 追加开始记录。
* 过程中：遇到报错、重要决策、文件变更时立即追加实时日志。
* 完成步骤：把 [-] 改为 [x]，并在 EXEC_LOG.md 追加完整归档（实际操作、验证结果、问题与解决、下一步建议）。


## 阶段 3：断点恢复（会话重置时强制）
当存在 TASK_PLAN.md 和 EXEC_LOG.md，或用户要求“继续任务 / 断点恢复”时：

1. 严禁重新规划或重复已完成步骤。
2. 完整阅读两个文件。
3. 找出第一个未完成的 [ ] 或 [-] 步骤。
4. 向用户汇报当前断点，然后直接从该步骤继续，并继续遵守阶段 2 规则。


## 额外强制约束

* 永远优先更新文件，再继续写代码。
* 日志必须足够详细，让另一个模型只读这两个文件就能无缝接手。
* 任务全部完成时，在 TASK_PLAN.md 顶部标记「已完成」，并写最终总结。

## 入口文档（P1 文档治理新增，本节不改三阶段协议）

* 新会话先读：`README.md`（中英双语入口）→ `docs/index.md`（导航）→ `docs/handbook.md`（Skill 手册现行）→ `docs/faq.md`。
* Skill 规范源：`skills/`（唯一源）；`.claude/skills`、`.opencode/skills`、`.agents/skills` 为本地链接，由 `scripts/install-skills.ps1` / `scripts/install-skills.sh` 重建，不入库。
* 任务跟踪表：`plan/` 下 dated 计划文件（时间+计划号命名），`TASK_PLAN.md` 为其精简投影。
* 日志有界：`EXEC_LOG.md` 根只留活跃周，旧账先读 `archive/README.md` 总索引再按需下钻；归档执行唯一走 `skills/banyan-archive/SKILL.md`（搬运三步原子，落盘后只读）。
* 冲突时以 `skills/<name>/SKILL.md` 为准。